
on: pull_request
name: CI
jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Force Xcode 12
      run: sudo xcode-select -switch /Applications/Xcode_12.1.app
    - name: Run tests
      run: xcodebuild clean test -project "PhotoGallery.xcodeproj" -scheme "PhotoGallery" -destination "platform=iOS Simulator,name=iPhone 11 Pro Max,OS=latest"
    - name: Build App
      run: xcodebuild -project "PhotoGallery.xcodeproj" -scheme "PhotoGallery" -destination "generic/platform=iOS" -configuration Release build CODE_SIGNING_ALLOWED=NO
          
